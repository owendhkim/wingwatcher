<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">

        <item
            android:id="@+id/home_menu"
            android:icon="@drawable/home"
            android:title="Home"/>

        <item
            android:id="@+id/info_menu"
            android:icon="@drawable/info"
            android:title="Info"/>

        <item
            android:id="@+id/privilages_menu"
            android:icon="@drawable/privilages"
            android:title="Privilages"/>
    </group>
    <group android:checkableBehavior="single">

        <item android:title="Authentication">
            <menu>
                <item
                    android:id="@+id/change_pass_menu"
                    android:icon="@drawable/password"
                    android:title="Change Password"/>

                <item
                    android:id="@+id/logout_menu"
                    android:icon="@drawable/logout"
                    android:title="Logout"/>
            </menu>
        </item>
    </group>
</menu>